# Data Directory

This folder documents the expected dataset layout. Raw videos are intentionally ignored by Git.

Use this structure locally:

```text
data/
├── raw_videos/
├── sample_frames/
├── annotations/
└── calibration/
```

Only commit lightweight examples that are safe to share, such as anonymized sample frames, small annotation files, or calibration templates.

